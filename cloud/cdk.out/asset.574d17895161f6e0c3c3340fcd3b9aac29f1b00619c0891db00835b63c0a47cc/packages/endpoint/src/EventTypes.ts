//
// Events — MOVED to @repo/system (the shared base both @repo/endpoint and @repo/cloud-manifest build
// on). Re-exported here so existing `import { Events } from "@repo/endpoint"` keeps working unchanged.
//
import { Events } from "@repo/system";
export default Events;

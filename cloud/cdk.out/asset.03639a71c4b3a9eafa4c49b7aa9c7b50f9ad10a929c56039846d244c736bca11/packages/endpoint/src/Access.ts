//
// Access — MOVED to @repo/system (the shared base both @repo/endpoint and @repo/cloud-manifest build
// on). Re-exported here so existing `import { Access } from "@repo/endpoint"` keeps working unchanged.
//
import { Access } from "@repo/system";
export default Access;

//
// Access — MOVED to @repo/events (the shared base both @repo/endpoint and @repo/cloud-manifest build
// on). Re-exported here so existing `import { Access } from "@repo/endpoint"` keeps working unchanged.
//
import { Access } from "@repo/events";
export default Access;

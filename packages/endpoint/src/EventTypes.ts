//
// Events — MOVED to @repo/events (the shared base both @repo/endpoint and @repo/cloud-manifest build
// on). Re-exported here so existing `import { Events } from "@repo/endpoint"` keeps working unchanged.
//
import { Events } from "@repo/events";
export default Events;
